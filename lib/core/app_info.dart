/// Metadados do app (mantidos em sincronia com `pubspec.yaml`).
class AppInfo {
  const AppInfo._();

  static const String name = 'DirectTube';
  static const String version = '0.1.0';
  static const String repository = 'https://github.com/HOWCKs/DirectTube';
}
